#!/bin/bash

set -euo pipefail

m events | while read -r line; do
    case "$line" in
        *'property-change: media-title'*)
            m c -s > /var/lib/hass-bridge/current-song
            ;;
    esac
done
